--178. Rank Scores--
SELECT
    score,
    DENSE_RANK() OVER (ORDER BY Score DESC) AS 'rank'
FROM
    scores
ORDER BY
    Score DESC;

--when you want to use as function give in single quotes so that we can use that as an hardcoded value, when the column has a same name.
